apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: l3-boundary-enforce1

on:
  workflow_dispatch:

jobs:
  scan-job:
    steps:
      - name: Enforce boundary test
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: echo "auto-pe-l3-boundary-enforce workflow executed"
